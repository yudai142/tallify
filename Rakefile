#!/usr/bin/env rake

require_relative 'config/boot'

require 'rake'

Rake.add_rakelib('lib/tasks') if Rake.application.tasks.empty?

require_relative 'config/application'

Rails.application.load_tasks
